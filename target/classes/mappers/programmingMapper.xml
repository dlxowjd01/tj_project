<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper

    PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"

    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  

<mapper namespace="com.tj.project.service.programmingMapper">
	<resultMap id="programming" type="com.tj.project.model.ProgrammingVO">
		<result property="pno" column="pno"/>
		<result property="writer" column="writer"/>
		<result property="subject" column="subject"/>
		<result property="contents" column="contents"/>
		<result property="nowdate" column="nowdate"/>
	</resultMap>

	<insert id="insertProgramming" parameterType="ProgrammingVO">

			INSERT INTO PROGRAMMING
				( ID
				  , PNO
				  , WRITER
				  , SUBJECT
				  , CONTENTS 
				  , NOWDATE )
			VALUES ( #{pno}
				  , #{writer}
				  , #{subject}
				  , #{contents}
				  , #{nowdate} )

	</insert>

	<update id="updateProgramming" parameterType="ProgrammingVO">

			UPDATE PROGRAMMING
			SET PNO=#{pno}
				, WRITER=#{writer}
				, SUBJECT=#{description}
				, CONTENTS=#{useYn}
				, NOWDATE=#{nowdate}
				  WHERE ID=#{pno}

	</update>

	<delete id="deleteProgramming" parameterType="int">

			DELETE FROM PROGRAMMING
			WHERE PNO=#{pno}

	</delete>

	<select id="selectProgramming" resultType="ProgrammingVO" parameterType="ProgrammingVO">

			SELECT
				PNO, WRITER, SUBJECT, CONTENTS, NOWDATE
			FROM PROGRAMMING
			WHERE PNO=#{pno}

	</select>

	<select id="selectProgrammingList" resultType="ProgrammingVO">

			SELECT
				PNO, WRITER, SUBJECT, CONTENTS, NOWDATE
			FROM SAMPLE
			ORDER BY ID ASC
	</select>


</mapper>
